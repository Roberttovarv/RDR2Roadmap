import { Mission } from "../../../types"

export const RenderMission = ({given_by, mission_en, mission_es, starting_at_en, starting_at_es, ending_at_en, ending_at_es, chapter, deadline, notes, completed}: Mission) => {
    return (
        <>
        </>
    )
}